[package]
name = "hexit"
version = "0.1.0"
authors = ["Hengo <leitner.andi@googlemail.com>"]
edition = "2021"

# TODO maybe update edition (changed some unsafe)

[dependencies]
anyhow = "1.0.98"
crossterm = { version = "0.29.0", features = ["osc52"] }
